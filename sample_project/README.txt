This is a minimalistic sample project that use pyrel.

This project is also used in pyrel's CI tests. So the packages are built from
it each time an update is pushed to https://github.com/rtmigo/pyrel.