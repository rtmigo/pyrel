This is a minimalistic sample project that use pyrel.

This project is also used in pyrel's CI tests. So the package is built from
this sample each time an update is pushed to https://github.com/rtmigo/pyrel